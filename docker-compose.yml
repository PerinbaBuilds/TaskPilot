version: "3.9"

services:
  taskpilot:
    build: .
    ports:
      - "8000:8000"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY:-}
    volumes:
      - .:/app
    working_dir: /app
    command: uvicorn api:app --host 0.0.0.0 --port 8000 --reload
